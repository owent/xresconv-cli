# Rust 重构计划

## 当前状态

2026-09-23：目标方向已确定为 Rust。本计划管理后续执行和验收，不表示已经开始实现。
基线为提交 `656c7e3d44efee978334e0364eb7de0fe3c8778c`；本轮开始时主工作区干净。
当前没有 Cargo 工程、Rust 测试或发布流水线，现有 README 的运行说明不能当作 Rust 使用说明。
证据、工具版本和适用主题见 [来源索引](doc/ai/source-index.md)。

## 阶段与验收

### P0：工程指引

- [x] 建立共享 `AGENTS.md`、两个按需 Skills、来源索引和文档验证约定。
- [x] 7 份 Markdown lint、两个 Skill frontmatter 与本地引用检查通过。
- [x] Kilo CLI 在仓库根和 `doc/ai` 的原生发现诊断均列出两个项目 Skill（`--pure` 会话）。
- [x] 补齐工程原则、PowerShell/现代工具、客户端和维护流程，以 [逐项覆盖表](doc/ai/requirements-coverage.md) 保存范围。
- [x] 补全后的 15 份文档、三个 Skill 格式/引用及 PowerShell 示例通过；Kilo 根/子目录均发现三个 Skill。
- [ ] 完成规则注入、新增 Skill 的 Codex 独立启动发现、目标客户端真实 Skill 路由及效果对照。
  静态检查与运行验证分别记录在 [验证记录](doc/ai/validation.md)。

### P1：冻结迁移合同

依赖：实施任务明确支持平台、兼容范围及可用的脱敏转换样本。

- [ ] 核对现有 CLI 参数、XML include 与配置合并、工作目录、输出矩阵和 Java 调度行为。
- [ ] 固定 xresconv-conf 参考提交、xresloader/JDK 版本；保存最小 XML 样本、预期命令流与产物。
- [ ] 明确 `--help`、`--version`、`--test`、非法并发数、缺失文件、失败退出码的目标合同。
- [ ] 对历史行为中的缺陷逐项决定保留还是修正，记录差异和回归场景，不默认复制偶然行为。

验收：每个兼容点有输入、可观察输出、失败预期和证据；有争议的行为先形成可审阅决定。
仅有源码推断而没有运行样本的条目明确标为未验证。

### P2：Rust 工程与首个完整流程

依赖：P1 的最小合同已明确。

- [ ] 确定 Rust edition、最低支持版本、目标平台和工具链固定方式；评估依赖维护、许可与必要性。
- [ ] 建立 `Cargo.toml`、`Cargo.lock`、源码和测试入口；将产物目录纳入忽略规则。
- [ ] 实现单个转换列表的解析、执行计划和无副作用预览，验证 Java 参数与 stdin 命令边界。
- [ ] 建立格式、静态检查、测试及 lint 门禁，记录实际执行证据并更新根规则中的状态。

建议先采用单个 package，区分配置解析、命令规划和执行职责；是否拆 crate 或引入异步运行时由实现需求决定。
这些是设计候选，尚未选定模块布局或第三方 crate。

验收：真实 CLI 测试证明预览不启动后端、不生成导出文件；路径、选项和错误输出均有断言。

### P3：兼容、并发与后端集成

依赖：P2 可运行；有固定版本的测试后端与输入。

- [ ] 补 include、重复配置、默认 scheme、多数据源、多协议文件、tag/class 和输出矩阵测试。
- [ ] 补子进程启动失败、非零退出、大量 stdout/stderr、取消、超时和进程回收测试。
- [ ] 验证并发上限、任务分配及失败汇总，覆盖非法并发数，避免静默漏任务。
- [ ] 在确定的平台上运行固定版本 xresloader 集成测试；对比导出内容并记录差异。

验收：契约测试和目标平台集成通过；mock 证据与真实后端结果分开，未测平台不标为支持。

### P4：发布准备与替换

依赖：P3 验收完成。

- [ ] 确定制品名、版本、目标平台、校验信息、后端获取方式和发行说明。
- [ ] 更新 README 的 Rust 安装与使用说明、示例和兼容差异，保留必要历史记录。
- [ ] 在干净目录验证制品、帮助/版本、预览和一次真实转表；指定替换失败的回滚条件与上一版制品。
- [ ] 准备最小权限 CI；第三方 Actions 固定完整提交，发布凭据只供可信发布任务使用。

验收：制品可复现、冒烟步骤可执行、回滚可用。发布准备和实际发布分别记录；本计划不自动授权发布。

## 待决策与交接

- 团队完整客户端清单待确认；当前依据本会话与本机检测，暂覆盖 Codex 和 Kilo Code CLI。
- 支持平台、最低 Rust 版本、JDK/xresloader 版本、发布形式及严格兼容范围尚未确定。
- 下一步是 P1 的行为样本与合同；不要为填充计划而提前创建空模块、空测试或发布配置。
- 长任务交接更新对应阶段、证据和下一步；架构决定在需要时建立一份权威设计并链接，避免另建平行路线图。
